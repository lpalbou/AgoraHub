{
  "//": "Cursor MCP config. Place at <workspace>/.cursor/mcp.json. Give EACH agent",
  "//identity": "its own Cursor workspace/window so it has its own key = its own identity.",
  "//secret": "Do NOT commit the real key. Put it in .cursor/agora.env (gitignored) and",
  "//secret2": "reference it, or inline it here in a gitignored copy. One key per workspace.",
  "mcpServers": {
    "agora": {
      "command": "agora-mcp",
      "env": {
        "AGORA_URL": "http://127.0.0.1:8765",
        "AGORA_API_KEY": "agora_PUT_THIS_AGENTS_KEY_HERE"
      }
    }
  }
}
